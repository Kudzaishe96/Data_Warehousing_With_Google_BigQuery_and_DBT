packages:
  - package: dbt-labs/dbt_external_tables
    version: 0.12.2

# rub dbt deps
#dbt run-operation stage_external_sources --vars "ext_full_refresh: true" to create the table
#dbt clean and then dbt deps